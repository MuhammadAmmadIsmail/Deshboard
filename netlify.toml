[build]
  publish = "build"           # or your app’s output directory
  command = "pip install -r requirements.txt"

[build.environment]
  MISE_PYTHON_COMPILE = "false"  # prevents mise from trying to compile Python from source
